<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:context=".MainActivity">

    <item android:id="@+id/getPopularMovies"
        android:title="PopularMovies"
        android:icon="@android:drawable/ic_menu_preferences"
        app:showAsAction = "never|withText"/>

    <item android:id="@+id/getTopRatedMovies"
        android:title="TopRatedMovies"
        android:icon="@android:drawable/ic_menu_add"
        android:orderInCategory="10"
        app:showAsAction = "never|withText"
        />

    <item android:id="@+id/getUpcomingMovies"
        android:title="UpcomingMovies"
        android:icon="@android:drawable/ic_search_category_default"
        android:orderInCategory="5"
        app:showAsAction = "never|withText"/>

    <item android:id="@+id/getMovieDetails"
        android:title="Buscar"
        android:icon="@android:drawable/ic_search_category_default"
        android:orderInCategory="5"
        app:showAsAction = "always"/>

</menu>